import { AdminDashboard } from '../../components/admin-dashboard';

export function AdminPricing() {
  return <AdminDashboard defaultTab="pricing" pageTitle="Pricing" pageDescription="Manage pricing plans and settings" />;
}
