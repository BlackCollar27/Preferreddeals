import { AdminDashboard } from '../../components/admin-dashboard';

export function AdminDistributors() {
  return <AdminDashboard defaultTab="distributors" pageTitle="Distributors" pageDescription="Manage distribution partner accounts" />;
}
