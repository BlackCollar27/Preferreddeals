import { AdminDashboard } from '../../components/admin-dashboard';

export function AdminUsers() {
  return <AdminDashboard defaultTab="users" pageTitle="Users" pageDescription="Manage user accounts and activity" />;
}
