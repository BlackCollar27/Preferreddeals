import { AdminDashboard } from '../../components/admin-dashboard';

export function AdminApprovals() {
  return <AdminDashboard defaultTab="approvals" pageTitle="Approvals" pageDescription="Review and approve pending items" />;
}
