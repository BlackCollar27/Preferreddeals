import { AdminDashboard } from '../../components/admin-dashboard';

export function AdminLocations() {
  return <AdminDashboard defaultTab="locations" pageTitle="Locations" pageDescription="Manage business locations and addresses" />;
}
